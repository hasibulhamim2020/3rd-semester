#include <stdio.h>
int main()
{
     int num1,num2,num3,sum=0;
     scanf("%d %d %d", &num1, &num2, &num3);
     sum = (num1+num2+num3);
     printf("Add of three Integer value are : %d\n", sum);
     return 0;
}

/*Write a program to add three integer values and print the result.*/
